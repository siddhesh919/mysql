SELECT 
    COUNT(salary)
FROM
    salaries;

SELECT 
    COUNT(from_date)
FROM
    salaries;

SELECT 
    COUNT(distinct from_date)
FROM
    salaries;    
    
SELECT 
    COUNT(*)
FROM
    salaries;
    